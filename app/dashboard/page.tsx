import Link from "next/link";import AppShell from "@/components/AppShell";import {listAkta} from "@/lib/store";import {rupiah} from "@/lib/constants";import {pihakNama} from "@/lib/akta";
export const dynamic="force-dynamic";
export default async function Dashboard(){const data=await listAkta();const not=data.filter(x=>x.kategori==="NOTARIS").length;const ppat=data.length-not;const total=data.reduce((s,x)=>s+x.honorarium,0);return <AppShell><section className="hero"><div><h1>Dashboard Administrasi</h1><div>Kelola arsip digital, akta Notaris, PPAT, honorarium, dan laporan.</div></div><Link className="btn btn-light" href="/akta/baru">+ Buat Akta Baru</Link></section><div className="grid stats"><div className="card stat">Total Akta<b>{data.length}</b></div><div className="card stat">Akta Notaris<b>{not}</b></div><div className="card stat">Akta PPAT<b>{ppat}</b></div><div className="card stat">Total Honorarium<b style={{fontSize:20}}>{rupiah(total)}</b></div></div><div className="card"><h2>Akta Terbaru</h2><div className="table-wrap"><table className="table"><thead><tr><th>Nomor</th><th>Tanggal</th><th>Jenis</th><th>Pihak</th><th>Status</th></tr></thead><tbody>{data.slice(-5).reverse().map(x=><tr key={x.id}><td><Link href={`/akta/${x.id}`} style={{color:'#1556ee'}}>{x.nomorAkta}</Link></td><td>{x.tanggal}</td><td>{x.jenisAkta}</td><td>{pihakNama(x)}</td><td><span className={`badge ${x.status==='Selesai'?'green':'gray'}`}>{x.status}</span></td></tr>)}</tbody></table></div></div></AppShell>}
